(function () {
    window.APP_DATA = window.APP_DATA || {};
    window.APP_ORDER = window.APP_ORDER || [];
    const id = 'cyber_security';
    if (!window.APP_ORDER.includes(id)) window.APP_ORDER.push(id);

    window.APP_DATA[id] = {
        id,
        name: 'Cyber Security',
        desc: 'Angreifermodelle, Netz- und Web-Sicherheit, Authentisierung, Krypto-Praxis, Incident Response.',
        formulas: `
            <strong>Schutzziele (CIA-Triade)</strong><br>
            <em>Confidentiality, Integrity, Availability</em>; Erweiterung: Authenticity, Non-Repudiation, Accountability.<br><br>

            <strong>Threat-Modeling: STRIDE</strong><br>
            Spoofing &middot; Tampering &middot; Repudiation &middot; Information&nbsp;Disclosure &middot; Denial&nbsp;of&nbsp;Service &middot; Elevation&nbsp;of&nbsp;Privilege<br><br>

            <strong>Risk</strong><br>
            $\\text{Risk} = \\text{Likelihood} \\times \\text{Impact}$ &nbsp; (qualitativ üblich; quantitativ über ALE = SLE × ARO).<br><br>

            <strong>Passwort-Hashing (Stand 2025)</strong><br>
            Empfehlung: <strong>Argon2id</strong> (RFC 9106) oder <strong>scrypt</strong>; Fallback: <strong>bcrypt</strong> (Cost ≥ 12) oder PBKDF2-HMAC-SHA-256 (≥ 600 000 Iterationen, OWASP 2023). <strong>Nie</strong> nur SHA-256/MD5/SHA-1.<br><br>

            <strong>TLS-Handshake (TLS 1.3, RFC 8446)</strong><br>
            ClientHello → ServerHello + Cert + CertVerify + Finished → ClientFinished (1-RTT). PSK/0-RTT optional. AEAD-Cipher only (AES-GCM, ChaCha20-Poly1305).<br><br>

            <strong>Session-Token-Entropie</strong><br>
            Erforderliche Bits gegen Online-Brute-Force: $\\text{Bits} \\ge \\log_2(\\text{Versuche}/\\text{Sekunde} \\cdot \\text{Lebensdauer}) + 64$ Sicherheitsmarge. Praxis: ≥ 128&nbsp;Bit zufällig (CSPRNG).<br><br>

            <strong>OWASP Top 10 (2021)</strong><br>
            A01 Broken Access Control · A02 Cryptographic Failures · A03 Injection · A04 Insecure Design · A05 Security Misconfiguration · A06 Vulnerable Components · A07 Identification &amp; Authentication Failures · A08 Software &amp; Data Integrity Failures · A09 Logging &amp; Monitoring Failures · A10 SSRF.<br><br>

            <strong>SQL-Injection-Defense</strong><br>
            Parameterized Queries / Prepared Statements; ORM mit Bind-Variablen; <strong>nie</strong> String-Konkatenation.<br><br>

            <strong>XSS-Defense</strong><br>
            Kontextsensitives Output-Encoding (HTML, Attribut, JS, URL, CSS), CSP <code>script-src \'self\'</code> (kein <code>\'unsafe-inline\'</code>), HttpOnly + SameSite=Lax/Strict für Session-Cookies.<br><br>

            <strong>CSRF-Defense</strong><br>
            SameSite-Cookies (Lax/Strict), Synchronizer-Token-Pattern oder Double-Submit-Cookie, Origin-/Referer-Check für State-ändernde Requests.<br><br>

            <strong>JWT-Best-Practice</strong><br>
            <code>alg</code> serverseitig fixieren (nie <code>none</code>, nie blind RS256/HS256-Switch); kurze Lebensdauer; Refresh-Tokens rotieren; Signatur immer prüfen; sensitive Daten nicht in Payload (Base64URL ist <em>kein</em> Verschlüsseln).<br><br>

            <strong>Kerberos-Grundfluss (RFC 4120)</strong><br>
            Client → KDC: AS-REQ → AS-REP (TGT). Client → KDC: TGS-REQ (TGT) → TGS-REP (Service-Ticket). Client → Service: AP-REQ. Mutual Auth: AP-REP.<br><br>

            <strong>OAuth 2.1 / OIDC (RFC 9700, 2024)</strong><br>
            Authorization Code Flow + PKCE für alle Clients. Implicit Flow ist deprecated. ID-Token = OIDC; Access-Token = OAuth-Ressourcenzugriff. Audience &amp; Issuer prüfen.<br><br>

            <strong>Defense in Depth / Zero Trust</strong><br>
            "Never trust, always verify." Identitäts-, Geräte- und Kontextprüfung pro Request; Mikro-Segmentierung; Least Privilege; kontinuierliche Telemetrie (NIST SP 800-207).<br><br>

            <strong>Incident-Response (NIST SP 800-61r2)</strong><br>
            Phasen: <em>Preparation → Detection &amp; Analysis → Containment → Eradication → Recovery → Post-Incident Lessons Learned</em>.
        `,
        levels: [
            // Level 1 — Grundlagen
            [
                {
                    q: 'Erkläre die <strong>CIA-Triade</strong> und gib für jedes Schutzziel ein konkretes Verletzungsbeispiel.',
                    h: 'Confidentiality, Integrity, Availability — jeweils typischer Angriff/Vorfall.',
                    s: '<strong>Confidentiality</strong> (Vertraulichkeit): Daten nur Berechtigten zugänglich. Verletzung: Datenleck (z.B. unverschlüsselte DB-Backups öffentlich auf S3-Bucket → Equifax 2017).<br><strong>Integrity</strong> (Integrität): Daten unverändert/authentisch. Verletzung: Man-in-the-Middle modifiziert Banküberweisungs-Empfänger; Manipulation von Logs nach einem Einbruch.<br><strong>Availability</strong> (Verfügbarkeit): Dienst erreichbar. Verletzung: DDoS-Angriff legt Webshop lahm; Ransomware verschlüsselt Server.<br>$\\boxed{\\text{Schutz}=\\{C,I,A\\}}$, oft erweitert um Authenticity und Non-Repudiation.'
                },
                {
                    q: 'Was bedeutet <strong>Defense in Depth</strong>? Nenne drei Schichten in einem typischen Web-Anwendungs-Stack.',
                    h: 'Mehrere voneinander unabhängige Schutzmechanismen, sodass das Versagen einer Schicht nicht den Gesamtschutz aufhebt.',
                    s: 'Prinzip: Mehrere voneinander <strong>unabhängige</strong> Schutzschichten, damit ein einzelner Fehler keine vollständige Kompromittierung erlaubt. Typischer Web-Stack:<ol><li><strong>Netzwerk</strong>: Firewall / WAF, Rate-Limiting, TLS-Termination.</li><li><strong>Anwendung</strong>: Input-Validation, Output-Encoding, Authentisierung (MFA), Autorisierung (RBAC/ABAC), Session-Management.</li><li><strong>Daten</strong>: Verschlüsselung at-rest (KMS), Least-Privilege-DB-User, parametrisierte Queries, Backups offline.</li></ol>Ergänzt durch Logging/Monitoring (SIEM) und organisatorisch durch Awareness/Patching/IR-Plan. $\\boxed{\\text{Resilienz} \\approx \\prod (1-p_i)}$ — je mehr unabhängige Schichten, desto kleiner die Restkompromittierungswahrscheinlichkeit.'
                },
                {
                    q: 'Was ist der Unterschied zwischen <strong>Authentisierung</strong>, <strong>Authentifizierung</strong> und <strong>Autorisierung</strong>?',
                    h: 'Reihenfolge im Anmeldeprozess. Wer behauptet was — wer prüft — wer darf was.',
                    s: '<strong>Authentisierung</strong> (engl. <em>authentication</em>, Subjekt-Seite): Nutzer legt Identitätsnachweis vor (Passwort, Token, Biometrie). Faktoren: <em>Wissen</em>, <em>Besitz</em>, <em>Inhärenz</em> (MFA = ≥ 2 verschiedene).<br><strong>Authentifizierung</strong> (System-Seite): System prüft den Nachweis und stellt fest „Identität ist X". Hashvergleich, Signaturprüfung, Challenge-Response.<br><strong>Autorisierung</strong>: System entscheidet, <em>welche</em> Ressourcen/Aktionen die nun bekannte Identität nutzen darf (RBAC, ABAC, ACLs).<br>Reihenfolge: <em>Authn → Authz</em>. Ein gültig authentifizierter Nutzer kann trotzdem für eine Aktion <em>nicht</em> autorisiert sein. Hinweis: im Deutschen werden Authentisierung und Authentifizierung oft synonym verwendet — wichtig ist die saubere Trennung von <em>Identitätsprüfung</em> und <em>Berechtigungsprüfung</em>.'
                },
                {
                    q: 'Welche Eigenschaften muss ein <strong>Passwort-Hash</strong> in 2025 haben? Warum ist <code>SHA-256(passwort)</code> nicht ausreichend?',
                    h: 'Salt, Pepper, Cost-Faktor, Memory-Hard. GPU-/ASIC-Angriffe.',
                    s: 'Kryptografische Hashes (SHA-2/3) sind <strong>schnell</strong> — genau das macht sie für Passwörter ungeeignet: eine moderne GPU testet Milliarden SHA-256/s. Anforderungen an einen Passwort-Hash:<ol><li><strong>Salt</strong> (≥ 128 Bit, pro Nutzer eindeutig, zufällig, im Klartext gespeichert) — verhindert Rainbow-Tables und Cross-User-Angriffe.</li><li><strong>Cost-Faktor</strong> (Iterationen / Time-Cost) — auf Server-Hardware ca. 50–250&nbsp;ms anstreben.</li><li><strong>Memory-Hardness</strong> — verteuert GPU-/ASIC-Parallelisierung (Argon2id, scrypt).</li><li>Optional <strong>Pepper</strong> (HSM-/Server-Geheimnis, getrennt vom DB-Salt).</li></ol>Empfehlung 2025 (OWASP/NIST):<ul><li><strong>Argon2id</strong> (RFC 9106) — Default. Beispiel-Parameter: $m=64\\,\\text{MiB}$, $t=3$, $p=1$.</li><li>Alternativ <strong>scrypt</strong> ($N=2^{17}$, $r=8$, $p=1$) oder <strong>bcrypt</strong> (Cost 12).</li><li>PBKDF2-HMAC-SHA-256 nur bei FIPS-Zwang, mit ≥ 600 000 Iterationen.</li></ul>$\\boxed{\\text{Schwach: } \\texttt{SHA256(pw)} \\quad \\text{Stark: } \\texttt{Argon2id(pw, salt, params)}}$'
                },
                {
                    q: 'Was ist eine <strong>SQL-Injection</strong>? Zeige ein verwundbares Code-Beispiel und die korrekte Lösung.',
                    h: 'Daten werden als Code ausgeführt. Lösung: Trennung von Code und Daten via Prepared Statements.',
                    s: 'Eine SQL-Injection entsteht, wenn unvalidierte Eingaben in einen SQL-String <strong>konkateniert</strong> werden, sodass der Angreifer Teile des Statements steuern kann. <em>Verwundbar:</em><pre><code>// Python (unsicher)\nq = "SELECT * FROM users WHERE name=\'" + user + "\'"\ndb.execute(q)</code></pre>Eingabe <code>user = "x\' OR 1=1 --"</code> liefert alle Datensätze. <em>Korrekt — Prepared Statement / Parameter-Binding:</em><pre><code>db.execute("SELECT * FROM users WHERE name = %s", (user,))</code></pre>Hier wird <code>user</code> nie als SQL geparst, sondern als <em>Wert</em> übergeben. Ergänzend: Least-Privilege-DB-Account, Allow-Listing erlaubter Werte (z.B. für ORDER-BY-Spaltennamen, die nicht parametrisierbar sind), und <em>kein</em> dynamischer SQL-Bau im Frontend. SQL-Injection ist Teil von OWASP Top 10 (A03:2021 — Injection).'
                },
                {
                    q: 'Erkläre kurz <strong>HTTPS / TLS</strong>: Welche drei Sicherheitsgarantien liefert TLS, und welche nicht?',
                    h: 'Vertraulichkeit, Integrität, Authentizität — aber nicht Endpunkt-Sicherheit.',
                    s: 'TLS (RFC 8446 für TLS 1.3) sichert die <strong>Transport-Schicht</strong> zwischen zwei Endpunkten. Garantien:<ul><li><strong>Vertraulichkeit</strong>: AEAD-Verschlüsselung (AES-GCM, ChaCha20-Poly1305) — Lauscher sieht nur Chiffretext.</li><li><strong>Integrität</strong>: AEAD-Tag erkennt Modifikation/Reihenfolge-Änderung.</li><li><strong>Authentizität (mind. Server)</strong>: X.509-Zertifikatskette gegen vertrauenswürdige Root-CAs; optional Client-Cert für mTLS.</li><li><strong>Forward Secrecy</strong>: ephemere ECDHE-Schlüsselableitung — späterer Schlüsselverlust kompromittiert Vergangenheit nicht.</li></ul>TLS sichert <strong>nicht</strong>:<ul><li>Inhalts-Korrektheit oder Vertrauenswürdigkeit der <em>Anwendung</em> (eine kompromittierte oder bösartige Website liefert weiter „grünes Schloss").</li><li>Endpunkt-Integrität (Malware auf Client/Server).</li><li>Korrekte CA-Validierung bei falsch konfigurierten Clients (Cert-Pinning oder OS-Trust-Store wichtig).</li></ul>Praxis: TLS&nbsp;1.2+ erzwingen, TLS 1.0/1.1 deaktivieren, HSTS setzen, OCSP-Stapling, sichere Cipher-Suites (Mozilla SSL Config Generator, „intermediate" oder „modern").'
                }
            ],
            // Level 2 — Vertiefung
            [
                {
                    q: 'Ein Web-Login speichert Passwörter als $H = \\texttt{SHA-256}(\\texttt{salt} \\,\\|\\, \\texttt{pw})$ mit 16-Byte-Salt. Ein Angreifer erbeutet die DB. Schätze die Zeit für einen Brute-Force gegen ein 8-stelliges alphanumerisches Passwort (62 Zeichen) auf einer GPU mit $10^{10}$ SHA-256/s. Was wäre mit Argon2id ($t=3$, $m=64$&nbsp;MiB) anders?',
                    h: 'Suchraumgröße: $62^8$. Zeit = Hashes/Rate. Memory-Hardness senkt Rate dramatisch.',
                    s: 'Suchraum: $62^8 \\approx 2{,}18\\cdot 10^{14}$ Kombinationen. Im Mittel die Hälfte: $\\approx 1{,}09\\cdot 10^{14}$. Mit $10^{10}$ Hashes/s pro GPU:<br>$$t \\approx \\frac{1{,}09\\cdot 10^{14}}{10^{10}\\,\\text{s}^{-1}} \\approx 1{,}09\\cdot 10^{4}\\,\\text{s} \\approx 3\\,\\text{Stunden}$$ pro GPU für ein Passwort. Bei einer Botnet-Farm mit 1000 GPUs: ~10&nbsp;s. Salt verhindert nur Rainbow-Tables, nicht Online-Brute-Force pro Hash. $\\boxed{\\text{SHA-256(salt}\\|\\text{pw) ist für Passwörter unzureichend.}}$<br><br>Mit <strong>Argon2id</strong> ($t=3$, $m=64$&nbsp;MiB, $p=1$): die GPU-Rate kollabiert auf $\\sim 10^{2}$–$10^{3}\\,\\text{H/s}$ pro Karte (Memory-Bandwidth-bound). Selbe Rechnung:<br>$$t \\approx \\frac{1{,}09\\cdot 10^{14}}{10^{3}}\\approx 10^{11}\\,\\text{s} \\approx 3\\cdot 10^{3}\\,\\text{Jahre}$$pro Passwort und GPU. Ein 8-stelliges alphanumerisches Passwort wird damit gegen einen Offline-Datenbank-Leak zumindest <em>jahrelang</em> sicher — bei sehr langen, hochwertigen Passwörtern (≥ 12 Zeichen, Diceware-Phrasen) praktisch unbrechbar. Annahme: Schätzwerte für GPU-Raten variieren je nach Generation und Implementierung um eine Größenordnung — die Schlussfolgerung bleibt.'
                },
                {
                    q: 'Erkläre <strong>CSRF</strong> (Cross-Site Request Forgery) anhand eines Beispiels. Welche drei Verteidigungen gibt es und wann reicht <code>SameSite=Lax</code> nicht?',
                    h: 'Cookie wird automatisch mitgesendet. Token-Pattern, Origin-Check, SameSite.',
                    s: 'Beispiel: Nutzer ist auf <code>bank.example</code> eingeloggt (Session-Cookie). Auf <code>evil.example</code> liegt:<pre><code>&lt;form action="https://bank.example/transfer" method="POST"&gt;\n  &lt;input name="to" value="ATTACKER"&gt;&lt;input name="amount" value="1000"&gt;\n&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code></pre>Der Browser sendet das Cookie automatisch mit, weil es zu <code>bank.example</code> gehört → unautorisierte Überweisung.<br><br><strong>Verteidigungen:</strong><ol><li><strong>SameSite-Cookie-Attribut</strong> (<code>Lax</code> default seit Chrome 80): Cookie wird bei <em>Cross-Site-POST</em> nicht mitgesendet. <strong>Achtung:</strong> Lax sendet Cookie bei <em>Top-Level-Navigation-GET</em> (z.B. via <code>&lt;a&gt;</code> oder Window-Open) trotzdem — wenn die State-ändernde Aktion über GET geht (was sie nicht sollte!) oder die Anwendung „<em>Login-Redirects</em>" akzeptiert, ist Lax unzureichend → <code>SameSite=Strict</code>.</li><li><strong>Synchronizer-Token-Pattern</strong>: Server sendet im HTML-Form ein zufälliges, sessiongebundenes <em>CSRF-Token</em>; State-ändernde Endpoints prüfen es serverseitig. Robust, auch bei alten Browsern.</li><li><strong>Origin-/Referer-Header-Prüfung</strong> für State-ändernde Requests (Allow-List vertrauter Origins).</li></ol>Zusätzlich: Re-Authentifizierung für sensitive Aktionen, kurze Session-TTLs, GET niemals für State-Änderungen verwenden (REST-Konvention).<br><br>Modern: Custom-Header (z.B. <code>X-CSRF-Token</code>) ohne CORS-Whitelisting → nur Same-Origin-JS kann ihn setzen. Kombiniert mit <code>SameSite=Lax</code> in der Praxis ausreichend.'
                },
                {
                    q: 'Was ist eine <strong>Stored XSS</strong>, was eine <strong>Reflected XSS</strong>, was eine <strong>DOM-XSS</strong>? Welche Defenses greifen wo?',
                    h: 'Wo wird die Payload eingespeist und wo gerendert? Server-Encoding vs. Client-Sinks.',
                    s: '<strong>Reflected XSS</strong>: Payload steckt in einem Request-Parameter und wird vom Server unverändert in die Antwort eingebaut. Beispiel: <code>/search?q=&lt;script&gt;alert(1)&lt;/script&gt;</code> erzeugt eine Seite, die sofort den Script-Tag enthält. Der Angreifer verteilt den Link.<br><br><strong>Stored XSS</strong>: Payload wird vom Server <em>persistent gespeichert</em> (DB, Forenpost, Profil) und bei jedem Aufruf an alle Besucher ausgeliefert — höchste Reichweite/Schaden.<br><br><strong>DOM-XSS</strong>: Reine Client-Schwachstelle. JavaScript schreibt unvalidierte Daten (oft <code>location.hash</code>) in einen <em>Sink</em> wie <code>innerHTML</code>, <code>document.write</code>, <code>eval</code>. Der Server sieht die Payload eventuell nie (z.B. weil der Hash nicht gesendet wird).<br><br><strong>Defenses:</strong><ul><li><strong>Output-Encoding</strong> (kontextspezifisch!): HTML-Body, HTML-Attribut, JS, URL und CSS haben verschiedene Escape-Regeln. Standard-Bibliotheken nutzen (z.B. OWASP Java Encoder, DOMPurify, React (escapt automatisch außer bei <code>dangerouslySetInnerHTML</code>)).</li><li><strong>Content-Security-Policy (CSP)</strong>: <code>script-src \'self\'</code> ohne <code>\'unsafe-inline\'</code>, idealerweise mit Nonce/Hash. Verhindert Ausführung injizierter Inline-Scripts.</li><li><strong>HttpOnly + Secure + SameSite</strong> für Session-Cookies (begrenzt Schadenshöhe: Session-Stealing wird verhindert).</li><li><strong>Trusted Types</strong> (Chrome): erzwingt typisierte Sinks, blockt rohe Strings in <code>innerHTML</code>.</li><li><strong>Input-Validation</strong> (Allow-List) ist Verteidigung in der Tiefe — ersetzt Output-Encoding aber <em>nicht</em>.</li></ul>Reflected/Stored: Server-Encoding + CSP. DOM-XSS: Client-Encoding + Trusted Types + sichere APIs (<code>textContent</code> statt <code>innerHTML</code>).'
                },
                {
                    q: 'Erkläre den <strong>OAuth 2.0 Authorization-Code-Flow mit PKCE</strong>. Warum ist PKCE auch für vertrauliche Server-Clients heute Pflicht (RFC 9700, OAuth 2.1)?',
                    h: 'Code-Verifier/Challenge, Authorization Code Interception, Redirect-URI-Hijacking.',
                    s: '<strong>Ablauf (vereinfacht):</strong><ol><li>Client erzeugt zufälligen <em>code_verifier</em> ($v$, ≥ 43 Zeichen, base64url-CSPRNG) und sendet <em>code_challenge</em> $c=\\texttt{BASE64URL}(\\texttt{SHA-256}(v))$ + <code>code_challenge_method=S256</code> mit dem Authorization-Request an den AS.</li><li>User authentifiziert sich beim AS, AS leitet zurück mit <code>code</code> an die Redirect-URI.</li><li>Client tauscht beim Token-Endpoint <code>code</code> + <em>code_verifier</em> $v$ gegen Access-/Refresh-Token. AS prüft $\\texttt{SHA-256}(v) \\stackrel{?}{=} c$.</li></ol><strong>Schutzziel:</strong> Selbst wenn ein Angreifer den Authorization-Code abfängt (Logs, Referrer-Leak, mobile App-Redirect-Hijacking), kann er ohne den nur dem Client bekannten <em>code_verifier</em> kein Token einlösen.<br><br><strong>Warum auch für Server-Clients (RFC 9700, 2024 / OAuth 2.1):</strong><ul><li>Verteidigung in der Tiefe gegen Code-Interception über Logs, Browser-History, Proxy-Caches.</li><li>Schutz vor falsch konfigurierten Redirect-URIs / Open-Redirector-Ketten.</li><li>Vereinheitlicht Best-Practice — <em>alle</em> Clients (Public + Confidential) verhalten sich gleich.</li><li>Implicit-Flow ist deprecated; Resource-Owner-Password-Credentials-Flow ebenfalls. Authz-Code + PKCE ist der einzige empfohlene Flow für interaktive Anmeldung.</li></ul>Zusätzlich: <code>state</code>-Parameter gegen CSRF, exact-match Redirect-URIs (keine Wildcards), Audience- und Issuer-Validierung des resultierenden ID-Tokens.'
                },
                {
                    q: 'Welche typischen Fehler passieren bei <strong>JWT-basierter Authentisierung</strong>, und wie verhindert man sie?',
                    h: '<code>alg=none</code>, RS256↔HS256, fehlende Signaturprüfung, Replay, Logout.',
                    s: '<strong>Häufige JWT-Fallen:</strong><ol><li><strong><code>alg: "none"</code> akzeptieren</strong>: viele alte Libraries haben das per Default getan. Fix: Algorithmus serverseitig <em>fixieren</em>, niemals dem JWT-Header vertrauen.</li><li><strong>RS256↔HS256-Confusion</strong>: Server erwartet RS256 (asymmetrisch), Lib akzeptiert aber HS256 (symmetrisch) und nutzt den <em>Public Key</em> als HMAC-Key — den der Angreifer kennt. Fix: Liste erlaubter <code>alg</code> hart kodieren; Schlüsseltyp gegen Algorithmus prüfen.</li><li><strong>Signatur nicht oder falsch geprüft</strong>: Frontend liest <code>payload</code> ohne Verifikation oder Backend dekodiert nur. Fix: <code>verify</code> statt <code>decode</code>; Ablauf (<code>exp</code>), Issuer (<code>iss</code>), Audience (<code>aud</code>), Not-Before (<code>nbf</code>) prüfen.</li><li><strong>Sensitive Daten in Payload</strong>: Base64URL ist <em>nicht</em> Verschlüsseln. Fix: nur unsensible Claims; bei Bedarf JWE.</li><li><strong>Lange Lebensdauer + kein Logout-Mechanismus</strong>: JWTs sind self-contained und schwer zu widerrufen. Fix: kurze TTL (5–15 min) für Access-Token, Refresh-Token mit Rotation, optional Server-side <em>jti</em>-Blocklist.</li><li><strong>Speicherort im Browser</strong>: <code>localStorage</code> ist XSS-anfällig. Fix: HttpOnly-Cookie (mit SameSite, Secure) oder kurze In-Memory-Tokens.</li></ol>Empfehlung: wo möglich serverseitige Sessions mit zufälliger Session-ID + Server-Store statt selbsttragender JWTs für Browser-Apps. JWT bevorzugt für stateless Service-zu-Service-Authentisierung mit kurzen Lifetimes.'
                },
                {
                    q: 'Beschreibe das <strong>Threat-Modeling-Verfahren STRIDE</strong> an einem konkreten Beispiel: REST-API mit JWT-Auth und Postgres-Backend. Mind. ein Risiko + Mitigation pro Kategorie.',
                    h: 'STRIDE = Spoofing, Tampering, Repudiation, Information Disclosure, DoS, Elevation of Privilege.',
                    s: '<strong>System</strong>: Mobile-/Web-Client → REST-API (Stateless, JWT) → Postgres.<br><br><strong>STRIDE-Analyse:</strong><table><tr><th>Kategorie</th><th>Risiko</th><th>Mitigation</th></tr><tr><td><strong>S</strong>poofing</td><td>Angreifer fälscht JWT (alg=none, schwacher HMAC-Key).</td><td><code>alg</code> fixieren (RS256), Schlüsselverwaltung in HSM/KMS, kurze TTL, Audience-Check.</td></tr><tr><td><strong>T</strong>ampering</td><td>Request-Body manipuliert (z.B. Preis ändern).</td><td>TLS für Integrität in Transit, Server-side-Validation aller Werte (Allow-Lists, Schema), keine Vertrauensentscheidung anhand Client-gelieferter Felder (z.B. <code>isAdmin</code>).</td></tr><tr><td><strong>R</strong>epudiation</td><td>User behauptet, Aktion nicht ausgeführt zu haben (z.B. Bestellung).</td><td>Manipulationssichere Audit-Logs (append-only, signiert, off-site repliziert), zeitstempelbar, mit Subject-ID + Request-ID.</td></tr><tr><td><strong>I</strong>nformation Disclosure</td><td>Stack-Traces in Fehler-Responses, IDOR (User A sieht Daten von User B via <code>/orders/123</code>).</td><td>Generische Fehler-Responses, IDs nicht erratbar (UUID/HMAC-IDs), serverseitige Authz-Prüfung pro Resource (kein „security through obscurity"), DB-Encryption-at-Rest, Least-Privilege-DB-User.</td></tr><tr><td><strong>D</strong>enial of Service</td><td>Massen-Login, teure Queries (<code>OFFSET 1e9</code>), File-Upload-Floods.</td><td>Rate-Limits (z.B. Token-Bucket, pro IP/User/Endpoint), Query-Timeouts und Cost-Limits, Pagination-Caps, WAF/CDN, Auto-Scaling mit Budget-Alarmen.</td></tr><tr><td><strong>E</strong>levation of Privilege</td><td>Horizontaler Sprung (User A → Admin) durch Manipulation eines <code>role</code>-Cookies; SQL-Injection mit DB-Superuser.</td><td>Server-side Rolemapping aus DB, niemals Client-Trust; parametrisierte Queries; DB-Account ohne <code>SUPERUSER</code>; Container ohne Root; SecComp/AppArmor-Profile.</td></tr></table>Strukturieren über Datenflussdiagramm (DFD) mit Trust-Boundaries — pro Boundary STRIDE durchgehen.'
                }
            ],
            // Level 3 — Expertise
            [
                {
                    q: 'Erkläre den <strong>Lucky-13-Angriff</strong> auf TLS&nbsp;1.0/1.1 mit CBC-Mode. Warum wurde MAC-then-Encrypt zugunsten von AEAD-Cipher (TLS 1.3) abgeschafft?',
                    h: 'Padding-Oracle, Timing-Differenz im HMAC, Anzahl SHA-1-Blöcke abhängig von Padding.',
                    s: 'TLS 1.0/1.1 verwendet <em>MAC-then-Encrypt</em> mit CBC-Padding (PKCS#7) und HMAC-SHA-1: $C = E_K(P\\,\\|\\,\\text{MAC}\\,\\|\\,\\text{Pad})$. Beim Empfang wird zuerst entschlüsselt, das Padding entfernt, dann der HMAC geprüft.<br><br><strong>Lucky-13 (AlFardan/Paterson, 2013):</strong> Der HMAC-SHA-1 verarbeitet die Eingabe in 64-Byte-Blöcken; je nach Padding-Länge $p$ ändert sich die Anzahl der gehashten Blöcke (1, 2 oder mehr). Die Verarbeitungszeit unterscheidet sich um wenige Hundert Nanosekunden — über $\\sim 2^{23}$ ausgewählte Chiffretext-Manipulationen statistisch detektierbar. Damit wird der TLS-Empfänger zum <em>Padding-Oracle</em>, das Klartext byteweise leaked. Der Name kommt vom Faktor 13, der sich aus der Block-Mathematik ergibt ($2^{13}$ Anfragen pro byte unter idealen Bedingungen).<br><br><strong>Konsequenz:</strong><ul><li>MAC-then-Encrypt ist gegenüber Padding-Oracles strukturell anfällig — selbst „Constant-Time"-Implementierungen (RFC 7366 Encrypt-then-MAC) entschärfen das Problem nur teilweise.</li><li><strong>AEAD</strong> (Authenticated Encryption with Associated Data, z.B. AES-GCM, ChaCha20-Poly1305) löst <em>Vertraulichkeit + Integrität</em> in einem Primitiv: ungültiger Tag → sofortiger Abbruch <em>vor</em> jeder Klartext-Verarbeitung. Kein Padding nötig (Stream-Modus / Counter-Mode).</li><li>TLS 1.3 (RFC 8446) erlaubt <strong>nur noch AEAD-Cipher</strong> — CBC und Stream-Cipher RC4 wurden entfernt; ebenso Renegotiation und alte KEX wie statisches RSA.</li></ul>Praxis-Fix für TLS 1.2: AEAD-Cipher bevorzugen (<code>ECDHE-ECDSA-AES-GCM</code>, <code>ECDHE-RSA-CHACHA20-POLY1305</code>); CBC-Cipher deaktivieren oder mit Encrypt-then-MAC-Extension (RFC 7366) absichern.'
                },
                {
                    q: 'Was ist der Unterschied zwischen <strong>Spectre</strong> (v1, Bounds-Check Bypass) und <strong>Meltdown</strong>? Warum sind sie für Web-Browser und Multi-Tenant-Cloud relevant, und was sind die wichtigsten Mitigationen?',
                    h: 'Speculative Execution + Cache-Side-Channel; Meltdown bricht User/Kernel-Grenze, Spectre die In-Process-Grenze.',
                    s: '<strong>Gemeinsamer Kern:</strong> Moderne CPUs führen Befehle <em>spekulativ</em> aus (Branch-Predictor, Out-of-Order). Falsche Spekulationen werden architektonisch verworfen, hinterlassen aber <em>mikroarchitektonische</em> Spuren — vor allem im Cache. Über einen Side-Channel (z.B. Flush+Reload-Zeitmessung) lassen sich diese Spuren ausgelesen werden.<br><br><strong>Meltdown (CVE-2017-5754):</strong> Eine User-Mode-Instruktion lädt spekulativ einen Kernel-Adressbereich, bevor die Privilege-Prüfung greift. Der spekulativ geladene Wert beeinflusst über einen abhängigen Speicherzugriff den Cache → Auslesen über Timing. Bricht die <em>User/Kernel</em>-Grenze. Betroffen: Intel (Out-of-Order ohne Permission-Check), teilweise ARM. AMD ist gegen klassisches Meltdown immun.<br><br><strong>Spectre v1 (CVE-2017-5753):</strong> Ein Bounds-Check wird spekulativ <em>umgangen</em>: <code>if (i &lt; arr1_len) y = arr2[arr1[i] * 256]</code>. Trainiert man den Branch-Predictor zu „taken" und gibt $i$ &gt; <code>arr1_len</code>, lädt die CPU spekulativ <code>arr1[i]</code> aus dem In-Process-Speicher und nutzt es als Index in <code>arr2</code> — Cache-Zustand verrät das Geheimnis. Bricht damit <em>Same-Process</em>-Grenzen (z.B. Sandbox eines JS-Engines, Container, Hypervisor-Grenzen).<br><br><strong>Relevanz:</strong><ul><li>Browser: Spectre v1 erlaubt JS-Code, Cookies/Token-Inhalte aus dem Browser-Heap zu lesen, wenn Cross-Origin-Daten in den Adressraum geladen werden.</li><li>Cloud: Multi-Tenant-VMs auf demselben Host können Geheimnisse auslesen.</li></ul><strong>Mitigationen:</strong><ul><li>Hardware: KPTI / KAISER für Meltdown (eigene Page-Tables für Kernel/User), Mikrocode-Updates (IBRS/IBPB/STIBP) gegen v2.</li><li>Browser: Reduktion der Timer-Genauigkeit (<code>performance.now()</code> &gt;= 100&nbsp;µs), <strong>Cross-Origin-Isolation</strong> via <code>COOP/COEP/CORP</code>, Site-Isolation (Chrome) — sensitive Origins bekommen eigenen Prozess.</li><li>Compiler/JIT: Spekulations-Lfences nach Bounds-Checks; Software-Mitigationen in V8/JIT.</li><li>Cloud: keine geteilten physischen Cores für ungetrennte Tenants (Hyper-Threading abschalten in besonders sensitiven Setups).</li></ul>Lehre: Performance-Optimierungen, die für funktionalen Determinismus invisible sind, können dennoch Information leaken — Sicherheitsgrenzen müssen <em>auch mikroarchitektonisch</em> gelten.'
                },
                {
                    q: 'Beschreibe das <strong>Heartbleed</strong>-Problem (OpenSSL, CVE-2014-0160). Was war der Fehler im Code, was war die Auswirkung und wie hätte ein systematisches Vorgehen den Bug verhindert?',
                    h: 'Heartbeat-Extension, Length-Feld nicht gegen Payload-Länge geprüft, Speicher-Leak bis 64&nbsp;KiB.',
                    s: '<strong>Bug</strong>: TLS-Heartbeat-Extension (RFC 6520) erlaubt einen Keep-Alive-Ping, bei dem der Empfänger die mitgelieferte Payload spiegelt. Der OpenSSL-Code (Datei <code>ssl/d1_both.c</code>) hat das vom Sender gelieferte Length-Feld $L$ verwendet, ohne zu prüfen, dass $L$ der tatsächlichen Payload-Länge entspricht: <pre><code>memcpy(bp, pl, payload);  // payload = L vom Angreifer kontrolliert\nbp -&gt; reply mit L Bytes\n</code></pre>Der Angreifer schickte <code>payload_length = 64&nbsp;KiB</code>, lieferte aber nur 1 Byte echte Payload. Der Server kopierte 64&nbsp;KiB <em>angrenzenden Heap-Speicher</em> in die Antwort: privater Schlüssel, Session-Cookies, Klartext-Passwörter — alles, was zufällig im Prozessspeicher lag. <em>Stillschweigend, ohne Spuren in Logs.</em><br><br><strong>Auswirkung</strong>: Etwa 17 % aller TLS-fähigen Webserver weltweit waren betroffen; Millionen private Zertifikatsschlüssel mussten neu ausgestellt und revoziert werden. Sessions konnten von Angreifern übernommen werden, die den Speicher zur richtigen Zeit auslasen.<br><br><strong>Wie hätte man es verhindern können?</strong><ul><li><strong>Defensive Bounds-Checks</strong>: <code>L</code> niemals ungeprüft an <code>memcpy</code> übergeben. RFC 6520 §4 schreibt explizit vor, das Längenfeld zu validieren — wurde im OpenSSL-Code nicht umgesetzt.</li><li><strong>Memory-Safety</strong>: Sprachen mit Bounds-Checking (Rust, Go) oder Sanitizer-Builds (ASan, MSan) hätten den Out-of-Bounds-Read zur Build-/Test-Zeit aufgedeckt. Heute werden TLS-Stacks zunehmend in Rust geschrieben (rustls, aws-lc-rs).</li><li><strong>Fuzzing</strong>: Coverage-guided Fuzzer (libFuzzer, AFL) gegen den TLS-Parser hätten die Anomalie schnell gefunden — heute Standard (OSS-Fuzz integriert OpenSSL).</li><li><strong>Code-Review &amp; minimaler Funktionsumfang</strong>: Heartbeat war für DTLS gedacht, in TLS-über-TCP überflüssig. Mehr Code = mehr Angriffsfläche („Attack-Surface-Minimization"). Auf der Fork-Ebene reagierte das BoringSSL-Team mit dem Entfernen der Heartbeat-Extension.</li><li><strong>Privilege-Separation / Memory-Hygiene</strong>: Private Keys können in einem separaten Prozess oder HSM gehalten werden, sodass sie nicht im Adressraum des TLS-Workers liegen.</li></ul>Lehre: Sicherheits-kritische C-Code-Pfade brauchen mehrschichtige Verteidigung — strenger Code-Review, Sanitizer in CI, Fuzzing, Memory-safe-Sprachen wo möglich.'
                },
                {
                    q: 'Was ist eine <strong>Side-Channel-Attacke</strong> auf RSA via Timing/Power, und warum heißt die Gegenmaßnahme „Constant-Time"? Skizziere ein modernes Beispiel (z.B. RSAES-PKCS1-v1.5 / Bleichenbacher).',
                    h: 'Daten-abhängige Verzweigungen/Speicherzugriffe leaken über Zeit, Cache, Stromverbrauch.',
                    s: '<strong>Prinzip</strong>: Eine Crypto-Implementierung muss ihre Laufzeit und ihren Speicherzugriffspfad <em>unabhängig vom Geheimnis</em> halten. Datenabhängige Branches (<code>if (key_bit) ...</code>), tabellenbasierte Lookups (<code>S[input]</code>) oder frühe Aborts geben über Timing- und Cache-Side-Channel den Schlüssel preis.<br><br><strong>Bleichenbacher-Angriff (1998, weiterhin relevant: ROBOT 2017)</strong>: RSAES-PKCS1-v1.5-Padding. Server entschlüsselt Client-Key-Exchange im TLS-Handshake; bei <em>ungültigem</em> Padding gibt er einen Fehler zurück. Diese Antwort wird zum Padding-Oracle: der Angreifer schickt $\\sim 10^6$ angepasste Chiffretexte und engt aus den Ja/Nein-Antworten den Klartext (das TLS-Premaster-Secret) bitweise ein — über die Multiplikativität von RSA: $c\\prime = c\\cdot s^e \\bmod n \\Rightarrow m\\prime = m\\cdot s \\bmod n$.<br><br><strong>Neuere Varianten</strong>:<ul><li>Timing-Bleichenbacher (verschiedene Latenzen für „Padding ok, aber andere Prüfung schlägt fehl" vs. „Padding-Fehler") — beobachtet z.B. bei Hardware-Load-Balancern.</li><li>ROBOT (2017): Mehrere Implementierungen (F5, Citrix, Cisco) waren noch verwundbar.</li></ul><strong>Constant-Time-Mitigationen</strong>:<ul><li>Padding-Prüfung <em>immer</em> in derselben Zeit; Fehler nicht früh signalisieren, sondern intern markieren und am Ende uniform handhaben.</li><li>Bei TLS 1.2-RSA-KEX: bei Padding-Fehler ein <em>zufälliges</em> Premaster-Secret zurückliefern → der Handshake schlägt erst beim Finished-Hash fehl, ohne Oracle-Information.</li><li>RSA-PKCS1-v1.5 für Encryption ist generell <em>deprecated</em> — moderne Stacks nutzen <strong>RSA-OAEP</strong> (für KEM) oder gleich ECDHE (TLS 1.3).</li><li>Allgemein: <em>kein</em> datenabhängiges Branching, <em>keine</em> Lookup-Tabellen mit geheimen Indizes (AES-NI, S-Box als arithmetische Funktion), Bitmasking für bedingte Auswahl, Sicherheitsbeweise unter dem „Constant-Time"-Modell.</li><li>Tools: <em>ctgrind</em>, <em>ct-verif</em>, Compiler-Annotations (<code>__attribute__((constant_time))</code>) — Verifikation, dass keine geheimen Daten in Branch- oder Adressberechnungen einfließen.</li></ul>Lehre: Crypto-Korrektheit ≠ Crypto-Sicherheit. Implementierungen müssen explizit gegen Side-Channels gehärtet sein; Eigenbau ist hochriskant — ausschließlich auditierte Bibliotheken (libsodium, BoringSSL, Bouncy Castle, rustls) verwenden.'
                },
                {
                    q: 'Erkläre den Unterschied zwischen <strong>Detection</strong>, <strong>Containment</strong> und <strong>Eradication</strong> in einem Ransomware-Incident-Response-Plan (NIST SP 800-61r2). Welche Fehler werden in der Praxis am häufigsten gemacht?',
                    h: 'Erkennung → Eindämmen → Beseitigen → Wiederherstellen → Lessons. Forensik vs. Hektik.',
                    s: '<strong>NIST 800-61r2 Phasen:</strong> <em>Preparation → Detection &amp; Analysis → Containment, Eradication &amp; Recovery → Post-Incident Activity</em>.<br><br><strong>Detection &amp; Analysis</strong>: Indikatoren (IDS/EDR-Alerts, ungewöhnlich viele File-Modifications, gelöschte Volume-Shadow-Copies, neue Admin-Accounts). Triage: ist es echt? Scope? Patient Zero? Welche Systeme sind kompromittiert, welche nur gefährdet? Forensische Datensicherung <em>vor</em> jeder destruktiven Aktion.<br><br><strong>Containment</strong>: Eindämmen, ohne Beweise zu vernichten. Stufen:<ul><li><em>Short-term</em>: Netzsegmentierung (VLAN-Quarantäne), Account-Disable, Block-IPs/Domains, Snapshots.</li><li><em>Long-term</em>: temporäre Workarounds, hardened Configs, getrennte Recovery-Umgebung.</li></ul>Bei Ransomware speziell: laterales Movement stoppen (SMB/RDP einschränken), C2-Kanäle blockieren, AD-Replikation prüfen — <em>nicht</em> sofort herunterfahren, da Memory-Forensik wertvoll ist.<br><br><strong>Eradication</strong>: Ursache und Persistenz vollständig entfernen. Reine AV-Säuberung reicht <em>nie</em>: bei Ransomware sind oft Backdoors, kompromittierte Service-Accounts, geänderte GPOs, eingenistete Webshells im Spiel. Strategie: <em>Rebuild from gold image</em>, alle Passwörter und Kerberos-Krbtgt-Tickets rotieren (zweimal!), Zertifikate revoziehen, Tier-0-Identitäten neu ausstellen.<br><br><strong>Recovery</strong>: Schrittweise Wiederinbetriebnahme aus <em>verifizierten</em> Backups (offline/immutable), erhöhter Monitoring-Pegel für Wochen, Service-Restart-Reihenfolge nach Abhängigkeiten.<br><br><strong>Häufige Praxisfehler</strong>:<ol><li><strong>Vorzeitiges Herunterfahren / Plattmachen</strong> — vernichtet Memory-Artefakte und Lateral-Movement-Pfade; man weiß nicht, wann der Angreifer wieder reinkommt.</li><li><strong>Unvollständige Eradication</strong>: AV-Lauf gemacht, Krbtgt-Hash aber nicht rotiert → Golden-Ticket-Persistenz, Angreifer ist Wochen später zurück.</li><li><strong>Backups erst im Ernstfall testen</strong> — sind unverschlüsselt, aber unvollständig oder zu alt, oder über dieselbe AD-Trust-Kette zerstörbar.</li><li><strong>Keine getrennte „War Room"-Kommunikation</strong>: man telefoniert über kompromittierte Teams/Mail-Systeme — Angreifer liest mit. Out-of-Band (Signal, Telefon, separate Cloud-Tenant).</li><li><strong>Lösegeld zahlen ohne Plan</strong>: keine Garantie auf Decryption, fördert das Geschäftsmodell, in vielen Jurisdiktionen sanktioniert (OFAC). Wenn Zahlung erwogen wird: Sanktionsprüfung, Forensik-/Recovery-Spezialist hinzuziehen.</li><li><strong>Lessons Learned überspringen</strong>: nach Recovery wird nicht dokumentiert, welche Detection-Lücke den Angreifer 47 Tage unbemerkt ließ — der nächste Vorfall folgt dem gleichen Pfad.</li></ol>Vorbereitung ist der größte Hebel: Tabletop-Übungen, getestete Runbooks, klare Rollen (IR-Lead, Comms, Legal, Forensics), 3-2-1-Backup mit immutable copies, EDR mit Telemetrie-Retention ≥ 90 Tage.'
                },
                {
                    q: 'Erkläre <strong>Zero-Knowledge-Authentication</strong> (z.B. SRP-6a oder OPAQUE) und warum das traditionelle „Server speichert Passwort-Hash, prüft beim Login" trotz Hashing nicht ideal ist.',
                    h: 'Phishing, Pre-Computation, Server-Compromise leakt Hash-Datenbank, kein Plaintext über Netz.',
                    s: '<strong>Problem klassischer Schemen:</strong> Selbst mit TLS und Argon2id sendet der Client beim Login das Passwort an den Server. Risiken:<ul><li>Phishing/MitM auf Client-Seite (kompromittiertes JS, falsche TLS-Konfiguration).</li><li>Server-Compromise: Angreifer sieht zukünftige Login-Passwörter im Klartext (bevor der Hash gebildet wird), nicht nur die Hash-DB.</li><li>Hash-DB-Leak ermöglicht Offline-Brute-Force gegen schwache Passwörter — egal wie teuer der Hash ist, gibt es immer eine Tail-Verteilung schwacher Passwörter.</li></ul><strong>Augmented PAKE (Password-Authenticated Key Exchange):</strong> Schemata wie <em>SRP-6a</em> (RFC 5054) oder <em>OPAQUE</em> (CFRG, RFC 9380/9497, 2024) erlauben gegenseitige Authentisierung <em>ohne</em> dass das Passwort den Client verlässt:<ul><li>Server speichert nur einen <em>Verifier</em>, nie ein direkt brute-forcebares „Passwort-Equivalent".</li><li>Client und Server berechnen gemeinsam einen Sitzungsschlüssel; falsches Passwort → kein Schlüssel-Match.</li><li><strong>OPAQUE</strong> nutzt einen Oblivious-PRF (OPRF), sodass weder ein passiver Beobachter noch der Server einen Brute-Force <em>vor</em> dem Server-Compromise gewinnen kann; selbst nach Compromise ist Brute-Force nur lokal pro Benutzer möglich (kein Pre-Compute über die DB).</li><li><strong>Phishing-Resistenz</strong>: Wird mit Endpunkt-Bindung (z.B. TLS-Channel-Binding, Origin) kombiniert, kann ein Angreifer-Server keine Credentials reuse-fähig abgreifen.</li></ul><strong>Vergleich zu WebAuthn/Passkeys:</strong> WebAuthn (FIDO2) löst dasselbe Problem auf einer anderen Ebene: asymmetrische Schlüssel pro Origin, Hardware-/Plattform-Authenticator. Phishing-resistent durch Origin-Bindung. In der Praxis ist <strong>Passkeys</strong> (synced WebAuthn) heute der pragmatischere Pfad für Endbenutzer-Login als PAKE-Roll-out.<br><br>Empfehlung: <em>Passkeys / WebAuthn</em> als Primary, klassisches Passwort + Argon2id als Fallback, OPAQUE/SRP nur in Spezial-Setups (z.B. Backup-/Recovery-Phrase, Hochsicherheitsbereiche). „Server speichert Hash" ist in 2025 nicht falsch, aber nicht mehr State-of-the-Art für hochwertige Identitäts-Systeme.'
                }
            ]
        ]
    };
})();
